#include "pch.h"
#include "Critter.h"

int Critter::getInput(int type) {
	return 0;
}

void Critter::doOutput(int type) {
}

void Critter::Think() {
		for (int i = 0; i < numConnections; i++) {
			CONN_T *x = &connection[i];
	}
}